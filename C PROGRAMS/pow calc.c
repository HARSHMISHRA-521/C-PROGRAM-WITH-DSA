#include<stdio.h>
#include<math.h>
int main(){
    int n,p,result;
    printf("eneter the no .\n");
    scanf("%d",&n);
    printf("eneter the power .\n");
    scanf("%d",&p);
    result=pow(n,p);
    printf("the result is %d \n",result);
return 0;
}